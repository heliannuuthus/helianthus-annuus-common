dependencies {
    api "org.springframework.boot:spring-boot-starter-webflux:${bootVersion}"
    api "org.springframework.boot:spring-boot-starter-log4j2:${bootVersion}"
}

configurations {
    all*.exclude module : 'spring-boot-starter-logging'
}
